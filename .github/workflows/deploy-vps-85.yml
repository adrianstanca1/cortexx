name: Deploy CortexBuild Pro to 85.190.100.68

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      VPS_HOST: 85.190.100.68
      VPS_USER: administrator
      VPS_SSH_KEY: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
      VPS_PASSWORD: ${{ secrets.VPS_ROOT_PASSWORD }}
      APP_URL: https://cortexbuildpro.tech
    steps:
      - name: Validate deployment credentials
        run: |
          set -euo pipefail
          if [[ -z "${VPS_SSH_KEY:-}" && -z "${VPS_PASSWORD:-}" ]]; then
            echo "::error::Set either VPS_SSH_KEY or VPS_PASSWORD in secrets."
            exit 1
          fi

      - name: Configure SSH
        run: |
          set -euo pipefail
          install -d -m 700 ~/.ssh
          {
            echo "85.190.100.68 ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIAAOE0euHxCIKpRJj5CETbE0X+4j3uoUKFp4m8xhVRGY"
            echo "85.190.100.68 ssh-rsa "
          } > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

          if [[ -n "${VPS_SSH_KEY:-}" ]]; then
            printf '%s\n' "$VPS_SSH_KEY" > ~/.ssh/id_ed25519
            chmod 600 ~/.ssh/id_ed25519
          else
            sudo apt-get update -qq
            sudo apt-get install -y -qq sshpass
          fi

      - uses: actions/checkout@v7

      - name: Deploy to VPS
        run: |
          set -euo pipefail
          SSH_OPTS="-o StrictHostKeyChecking=no -o ConnectTimeout=15"

          if [[ -n "${VPS_SSH_KEY:-}" ]]; then
            SSH_CMD="ssh ${SSH_OPTS} -i ~/.ssh/id_ed25519 ${VPS_USER}@${VPS_HOST}"
          else
            SSH_CMD="sshpass -p '${VPS_PASSWORD}' ssh ${SSH_OPTS} ${VPS_USER}@${VPS_HOST}"
          fi

          $SSH_CMD 'bash -s' << 'REMOTE'
            set -euo pipefail
            APP_DIR=/opt/cortexx

            echo "=== Pull latest code ==="
            cd "$APP_DIR"
            git fetch origin
            git reset --hard origin/main
            echo "Checked out: $(git rev-parse --short HEAD)"

            echo "=== Bring stack up ==="
            docker compose pull
            for up in 1 2 3; do
              sleep 5
              if docker compose up -d --build --remove-orphans 2>&1; then
                break
              fi
              echo "docker compose up attempt $up failed — retrying"
              sleep 10
            done

            echo "=== Wait for health ==="
            for n in $(seq 1 36); do
              code=$(curl -s --max-time 5 -o /dev/null -w "%{http_code}" https://cortexbuildpro.tech/api/health 2>/dev/null || echo 000)
              if [ "$code" = "200" ]; then
                echo "API healthy: HTTP 200"
                break
              fi
              echo "  try $n: HTTP $code"
              sleep 5
            done
          REMOTE

      - name: Verify deployment
        run: |
          sleep 10
          echo "=== Public ==="
          curl -s -o /dev/null --max-time 20 -w "HTTPS root   -> %{http_code}\n" https://cortexbuildpro.tech/
          curl -s -o /dev/null --max-time 20 -w "HTTPS health -> %{http_code}\n" https://cortexbuildpro.tech/api/health
          echo "=== Health body ==="
          curl -s --max-time 10 https://cortexbuildpro.tech/api/health
          echo ""
