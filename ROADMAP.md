# Cortexx — Roadmap

Status as of **24 May 2026**.

> **Repository consolidation (24 May 2026):** All Cortexx-related repositories (`cortexbuild-ultimate`, `cortexbuild-field`, `cortexbuild-web`, `cortexbuildpro`, `cortexbuild-platform`, `cortexbuild-unified`, `cortexbuildpro-ultimate`, `cortexxxxxx`, `cortexx-pwa`) have been merged into this repository. Their full source code is preserved in `archive/` — including `archive/cortexx-pwa/` (standalone single-file PWA bundles + app-store submission pack, 5 MB) and `archive/claude-design/` (the original Claude Design canvas JSX + chat history + reference screenshots, dated April 30). All redundant repositories have been deleted from GitHub. This is now the **single source of truth** for the entire Cortexx platform.

Source of truth for what's shipped, in-flight, and planned. Maintained alongside the codebase, not in a separate tracker.

## ✅ Shipped

| Phase | What landed |
|---|---|
| **Foundation** | Next.js 14 App Router, Postgres + Prisma, NextAuth credentials, 4 migrations, idempotent seed, ErrorBoundary, themed 404 + loading, error reporting to `/api/errors`. |
| **Mobile shell** | 5-tab TabBar (Dashboard / Projects / + / Tasks / Team), hamburger DrawerMenu with active-page highlight, QuickActions bottom-sheet from + FAB (6 actions), UserMenu, AuthedShell wrapper. |
| **15 dashboards** | ActionFirst, StatusBoard, Calm, Bento, AIForward, Field, Timeline, Money/Books, Stories, Rings, SiteMap, Focus, **Executive** (v13, cash-first KPI grid for the founder seat), **Broadsheet** (v14, newspaper aesthetic), **Site notice** (v15, hazard-tape construction noticeboard) — all matching the Claude design (gradient hero, blueprint SVG, accent palette, etc). |
| **Project surfaces** | List + filters (active/archived), detail with Overview/Tasks/Team/Finance tabs, Kanban board, gallery, archive (soft-delete + restore). |
| **Task surfaces** | List + search + filters, bulk select (complete/reopen/delete), category badges, comments + count, due-time picker, edit modal, optimistic toggles. |
| **Team surfaces** | List with empty-state CTA, per-member profile, edit modal, hours tracking. |
| **Capture flow** | Camera photo upload, Voice RFI → task, Receipt scan, Incident report, Check-In with onSiteCount + activity log. |
| **Money** | Invoices CRUD + print, paid/owed totals via `groupBy` aggregate, per-status filters, overdue badges. |
| **Productivity** | Workspace search across projects/tasks/team/invoices, Inbox with overdue/critical/expiring aggregation, Activity feed with filters, Reports (cashflow + project margins + tasks + activity), CSV export. |
| **Realtime** | SSE `/api/events/stream` with reconnect, `useRealtimeActivity` hook, live indicator dot in 10 of 15 dashboard variants (Action-first, Status board, Bento, AI-forward, Field, Timeline, Books, Stories, Rings, Map, Executive). Calm / Focus / Broadsheet / Site-notice intentionally skipped — their minimalist or typographic aesthetics would clash with a status dot. |
| **PWA** | Full icon set (192/512/maskable + apple-touch 152/167/180), 6 iPhone splash screens, manifest with shortcuts, service worker `cortexx-v3` with offline fallback + auto-update prompt + **push notification handlers** (push + notificationclick), iOS Add-to-Home-Screen install hint. |
| **Push notifications** | `PushSubscription` model + migration, `lib/push.ts` with VAPID + auto-prune of 410/404 stale endpoints, `/api/push/subscribe` (GET capability check / POST upsert / DELETE), `/api/push/test` for the Settings test button, Settings → Notifications UI with browser-support + server-config gates (degrades cleanly when VAPID isn't set). Subscriptions can be captured before keys are configured so users opt-in once and notifications start flowing when an admin sets `VAPID_PUBLIC_KEY` + `VAPID_PRIVATE_KEY` + `VAPID_CONTACT_EMAIL` env vars. |
| **Apps hub** | `/apps` route — modules grid matching the design's "CAPTURE + ALL APPS" layout, 9 CAPTURE actions with AI labels, 24 modules grouped by category, real-time badges. |
| **Snags** | `/snags` — real defect register: `Snag` model + migration, full CRUD (`/api/snags`, `/api/snags/[id]`), status pipeline (open → in_progress → closed), priority + due date, optional photo attachment via the uploads backend, per-project filtering, activity events on raise/close. Drives the `/apps` snag badge. |
| **Photos** | `/photos` — project-filterable photo gallery built on the uploads backend. Adds an upload action that creates a `type=photo` `Document`; renders a 3-column grid with per-tile project chip; opens full-size in a new tab. |
| **Timesheets** | `/timesheets` — real weekly grid on top of the existing `/api/timeentries` backend (uses `week`/`year`/`approved` filters). Mon–Sun day grid per member with totals + status pip, per-entry approve toggle, "Approve week" bulk action, prev/next week navigation, add-entry bottom-sheet (member + project + date + hours with 0&lt;h≤24 validation), per-entry delete with confirm, "send to payroll" composes a mailto with daily breakdown. Drives the `/apps` timesheets badge. Ported from `archive/cortexbuild-field/app/timesheets.tsx` (Manus-built) to the Next.js stack. |
| **Training & CSCS** | `/training` — `Certification` model + migration, full CRUD (`/api/training`, `/api/training/[id]`), status buckets (valid / expiring within 60 days / expired) with KPI strip + filter chips, common-type dropdown (CSCS, IPAF, PASMA, SSSTS, SMSTS, Asbestos, First Aid, Working at Heights), optional TeamMember link with name auto-fill, per-card delete with confirm. Drives the `/apps` training badge (count of expiring + expired). Ported from `archive/cortexbuild-field/app/training.tsx` (Manus). |
| **RFIs** | `/rfis` — `Rfi` model + migration with per-project `RFI-001` sequence numbering, full CRUD (`/api/rfis`, `/api/rfis/[id]`), status pipeline (open → answered → closed), priority + ball-in-court (freeform with common-assignee datalist) + due date with overdue detection (red ring on card), response/answer flow with `respondedAt` timestamp, activity events on raise / answer / close / reopen. Detail bottom-sheet shows full question + response form + status cycling. Drives the `/apps` rfis badge (open count). Ported from `archive/cortexbuild-field/app/rfis.tsx` (Manus). |
| **Messages (Announcements)** | `/messages` — `Announcement` model + migration, full CRUD (`/api/announcements`, `/api/announcements/[id]`), 4 types (general / safety / urgent / update) with auto-pinning for safety + urgent, optional project scope (whole workspace or a specific project), pinned-first ordering, per-card pin toggle + delete with confirm. The Manus version was a broadcast board — landed here as the Messages module since threaded conversations were always v1.5+ work. Ported from `archive/cortexbuild-field/app/announcements.tsx` (Manus). |
| **Site diary** | `/site-diary` — no new model. `/api/site-diary?projectId=&date=` aggregates a single project-day from existing data: `TimeEntry` (hours + headcount), `Activity` (audit feed), `Snag` (raised + closed that day), `Document` (photos + RAMS uploaded). 6-cell KPI grid + sectioned timeline (Hours / Snags / Photos / Activity), prev/next day navigation + jump-to-today, share-to-email composes a plain-text report. Ported from `archive/cortexbuild-field/app/daily-report.tsx` (Manus) — aggregation approach instead of a separate diary model. |
| **Observations** | `/observations` (new route, separate from `/snags`). `Observation` model + migration, full CRUD (`/api/observations`, `/api/observations/[id]`), 4 types (positive / improvement / unsafe / near_miss) with emoji + colour coding, status (open / resolved) with `resolvedAt` timestamp, type and status filters, per-card resolve toggle + delete, activity events on log / resolve / reopen. Drives the `/apps` observations badge (counts unsafe + near-miss still open — the safety-critical subset). Ported from `archive/cortexbuild-field/app/observations.tsx` (Manus). |
| **Variations** | `/variations` — `Variation` model + migration with per-project `VAR-001` sequence numbering, full CRUD (`/api/variations`, `/api/variations/[id]`), status pipeline (draft → submitted → approved/rejected, reopen path), cost impact (£) + days impact tracked per variation with approved-totals aggregate in the header, "Email for approval" composes a mailto to the client with full breakdown, activity events on every status transition. Drives the `/apps` variations badge (draft + submitted count). Manus archive had no variations.tsx — designed from scratch following the snags/rfis pattern. |
| **Sales pipeline** | `/leads`, `/customers`, `/quotes` — three new modules with shared `Lead` / `Customer` / `Quote` models in one migration (`20260524000300_add_lead_customer_quote`). **/leads** has a 5-stage Kanban view (new → qualified → proposing → won/lost) with pipeline value aggregate in the header and one-click "Won — convert" that creates a Customer in a transaction and stamps `convertedAt`. **/customers** has search + active/archived toggle, per-row quote count, archive/unarchive + delete in the detail sheet, deep-link "Quotes for this customer" via `/quotes?customerId=`. **/quotes** has an in-modal line-item editor (description, qty, unit, unit price, auto-computed total), live VAT + grand-total preview, sequential `QTE-NNNN` numbering, status pipeline draft → sent → accepted/rejected with `sentAt` / `acceptedAt` / `rejectedAt` stamps, "Send by email" composes a mailto to the customer's contact email with full line-by-line breakdown and auto-marks sent. Drives the `/apps` leads badge (in-pipeline count). |
| **Drawings + Schedule** | `/drawings` — new `Drawing` + `DrawingRevision` models with sequential `DWG-NNN` per project + per-drawing unique `(drawingId, revision)`. `POST /api/drawings/[id]/revisions` adds a revision pointing at a file uploaded via the existing `/api/uploads` (PDF / image / DWG, ≤ 25 MB). List grouped by project with status pip (draft / approved / superseded / archived), filter chips by project + discipline auto-built from distinct DB values. Detail bottom-sheet has the upload-rev form (auto-increments label A→B→C or P01→P02) + reverse-chrono revision history with "CURRENT" marker on the latest, "Open" deep-link to the uploaded file. Activity events on add / rev-upload / delete. **/schedule** — read-only `/api/schedule?from=&weeks=` aggregates Tasks with `dueDate` across active projects (window-snapped to Monday, configurable 4/8/12 weeks). Gantt-style horizontal day grid with sticky project column, today highlighted, Monday boundaries thicker, weekend columns dimmed. Each task renders as a coloured bar positioned by its dueDate with status background + priority left-stripe (critical/high/medium/low), clickable to `/tasks?focus=`. Prev/next-week + jump-to-today nav, project filter chips. |
| **Compliance & bids** | `/permits` — `Permit` model + `/api/permits`(+`/[id]`) — hot work / confined space / excavation / working at height / electrical permits, status (draft / active / expired / cancelled), risk level, valid-from + valid-to with expiry-alert bucket, issued by/to. `/rams` — `Rams` model + `/api/rams`(+`/[id]`) — Risk Assessment + Method Statements (UK construction-specific), hazards / controls / PPE fields, review-by date with reminder, signed-by + signed-at. `/tenders` — `Tender` model + `/api/tenders`(+`/[id]`) — bid pipeline (draft / submitted / won / lost / withdrawn), total value, deadline + submitted-at + decided-at timestamps. Each module also drives an `/apps` badge in the new "Compliance & bids" section. |
| **Milestones (back-end)** | `Milestone` model + `/api/milestones`(+`/[id]`) — planned start/end + actual end, status (planned / in_progress / complete / slipped). Surfaced via the `/schedule` overview and used to feed slipped-count badge calculations. No dedicated `/milestones` page yet — managed inline through project surfaces. |
| **Ask hardening (15 review findings)** | `lib/llm.ts` + `/api/ask` + `/ask` page tightened up: per-user namespaced `localStorage` (no cross-user leak on shared browsers), 401 → `/login?callbackUrl=/ask` redirect (was generic error), error bubbles pruned before being re-sent as LLM history (was re-feeding broken bubbles), `LlmEmptyResponseError` mapped to clean `502 LLM_EMPTY` (was misleading 503), in-process rate limit at 20 req / 60 s per user with proper `Retry-After`, prompt-injection guards (sanitised project names + activity context), `num_predict` cap (1024) so aborted requests don't tie up Ollama, IME-safe Enter handler (`composingRef` + `e.nativeEvent.isComposing` + legacy keyCode 229), 9 new unit tests in `test/ask-llm.test.js`. |
| **Client view (public share)** | `/client-view` (auth-gated link manager) + `/client/[token]` (public read-only project page). Adds `Project.shareToken @unique`. `POST /api/projects/[id]/share-token` generates a 16-char URL-safe token (alphabet excludes `0/O/1/l/I`) with retry-on-collision, `DELETE` revokes. Public `GET /api/client-view/[token]` returns only client-safe fields: name/address/progress/budget vs spent (not margin), open snag count (not titles), last 12 photos, last 10 activity events filtered to safe iconTypes. `middleware.ts` exempts `/client/` + `/api/client-view/` from auth. Manager UI: copy link / preview / email-to-client mailto / rotate / revoke-with-confirm. Public page: hero with progress bar, KPI strip (status / open snags / photos), budget bar, programme dates, photo grid, activity feed, "private link" footer. |
| **Ask Cortex (local LLM)** | `/ask` — chat UI backed by `/api/ask` and a local Ollama server. `lib/llm.ts` is a tiny Ollama-compatible client (60s timeout, clean `LlmUnavailableError` mapping to `503` with install/pull instructions, no half-baked fallback). Default model `llama3.2:3b` via `OLLAMA_BASE_URL` + `OLLAMA_MODEL` env. POST body `{ message, history[] }` — server builds a system prompt with live workspace context (active project names + count, open snags, pending TS, last 5 activity events) so answers are grounded. Chat UI stores last 40 messages in `localStorage`, suggested-prompt grid on first open, "Clear conversation" button, animated typing dots while waiting, model + latency footer under each AI reply. No API key needed — runs entirely on the VPS. |
| **Daily ops trio (toolbox talks / maintenance / suppliers)** | `/toolbox-talks`, `/maintenance`, `/suppliers` + one migration (`20260524001000_add_toolboxtalk_maintenance_supplier`) adding `ToolboxTalk`, `MaintenanceSchedule`, `Supplier` models. **/toolbox-talks**: log per-shift safety briefings with attendees list (count auto-derived from non-empty lines, capped at 500), optional project link, hazards covered, sign-off flow with `signedAt` stamp + activity event. Header shows month-to-date count. **/maintenance**: schedule services / inspections / calibrations / repairs against equipment with optional recurring `intervalDays`. Marking complete in a server `$transaction` (a) stamps `completedAt`, (b) auto-creates the next occurrence shifted by `intervalDays` if set, (c) rolls `Equipment.lastServicedAt` + `nextServiceAt` forward. Header surfaces overdue + due-in-7d counts. **/suppliers**: full materials / plant / services / other register with contact details, payment terms, account number, archive/restore + delete, search across name + contact name, mailto / tel quick-actions. Drives 2 new `/apps` badges (toolboxTalks = month count, maintenance = overdue count). |
| **Quality & governance trio (inspections / meetings / risks)** | `/inspections`, `/meetings`, `/risks` + one migration (`20260524000900_add_inspection_meeting_risk`) adding `Inspection`, `Meeting`, `Risk` models. **/inspections**: 5 inspection types (general/safety/quality/scaffold/electrical), each with a sensible starter checklist (4-5 items) auto-populated on schedule; checklist items toggle through pass/fail/N/A inline (auto-promotes `draft → in_progress` on first interaction), terminal status (passed/failed) stamps `overallResult` + `completedAt` once and re-passing preserves the original timestamp. **/meetings**: scheduled time + duration (5-480 min clamp), per-meeting expandable detail with editable minutes, attendees, action items checklist (toggle done, add via Enter, per-item assignee + due), status pipeline (scheduled → completed/cancelled). **/risks**: full project-level risk register with `category` (operational/financial/schedule/safety/quality/environmental), likelihood × impact = server-computed score (1-25), 5×5 heat-map visualising open + mitigated + accepted risk distribution at the top of the page, severity bands (LOW <8, MEDIUM ≥8, HIGH ≥15), 4-stage status (open → mitigated/accepted → closed). All three drive `/apps` badges (inspections = failed count, meetings = upcoming count, risks = HIGH-severity count). |
| **Money / materials trio** | `/materials`, `/pos`, `/sub-invoices` — three new modules + three new Prisma models in one migration (`20260524000600_add_material_po_subinvoice`). **/materials**: `Material` model with `stockLevel` + `reorderPoint`, low-stock filter chip + alert count, category chips, per-item stock adjust (+/− with delta entry), search across name/code/supplier. **/pos**: `PurchaseOrder` with sequential `PO-NNNN`, JSON `lineItems` with server-recomputed totals (subtotal/VAT/total), status pipeline draft → sent → received → closed (+ cancelled), `sentAt`/`receivedAt`/`closedAt` stamps, "Email supplier" composes a mailto pre-filled with the supplier email + full line-by-line breakdown and auto-marks sent on first send, header surfaces open count + committed value. **/sub-invoices**: `SubInvoice` model with HMRC CIS auto-deduction (gross=0%, registered=20%, unregistered/higher=30%) computed server-side from the linked subcontractor's `cisStatus` — sets `cisAmount`, `vatAmount`, `grossAmount`, `payableAmount` on every write. Add-form previews the breakdown live as the user types. Pending payable + CIS held aggregates in the header. CSV export for payroll/HMRC. Status pipeline received → approved → paid (+ disputed) with `paidAt` stamp. |
| **Asset / catalog trio** | `/cost-catalog`, `/subs`, `/equipment` — three new modules sharing one migration (`20260524000500_add_costitem_subcontractor_equipment`). **/cost-catalog**: `CostItem` model (code, description, category, unit, unitCost, vendor), search + category chips auto-built from distinct values, list grouped by category, click-to-edit-in-modal for both add and edit (same form). **/subs**: `Subcontractor` model with CIS status (gross / 20 / 30), UTR, insurance + qualifications expiry stamps. List highlights expiry alerts (red for expired, amber for expiring within 60 days). Detail sheet has expiry cards, mailto/tel links, archive/unarchive + delete. **/equipment**: `Equipment` model with ownership (owned/hired), hireCompany, location, status (in_service / in_yard / in_service_centre / out_of_service) + lastServicedAt / nextServiceAt with bucketed service alert (overdue / soon ≤30d / ok). Detail sheet has 4-button status grid for one-tap status change. |
| **Operations trio** | `/check-in`, `/live-status`, `/mileage` — `SiteCheckIn` + `MileageEntry` models + migration (`20260524000400_add_sitecheckin_mileage`). **/check-in**: optional GPS capture in + out, full CRUD via `/api/checkins`(+`/[id]`), atomic `$transaction` that flips `TeamMember.onSite` + bumps/decrements `Project.onSiteCount` on every in/out, prevents double check-in. **/live-status**: read-only aggregation via `/api/live-status` (open check-ins + active projects + off-site team), auto-refresh every 30s when tab visible, 4-cell KPI strip (on site / sites busy / sites empty / off site), groups team by site with first-name pill + hours-on-site, "empty sites" warning section, "off site" team chip cloud. **/mileage**: `/api/mileage`(+`/[id]`) with month + approved filter, HMRC rate per vehicle (car/van 45p, motorbike 24p), server-computed `amount = miles × ratePence / 100` on every write, validated bounds (miles ≤ 2000, rate ≤ 200p), CSV export of the visible month, per-entry approve toggle. |
| **A11y** | `aria-label` on icon-only buttons, `aria-current=page` on active nav, focus-trap on modals, Escape-to-close on bottom sheets, screen-reader-friendly toasts. |
| **Performance** | `dueTime`/`dueDate`/`skip`/`hours`/`progress` validation hardened, 4 graceful-404 endpoints, dashboard `groupBy` aggregates, static asset cache headers (30d for icons, immutable for `/_next/static`), CI build cache. |
| **Deploy reliability** | Hardened deploy script: SSH retry 8x, hard-fail on build error, `pm2 delete + start` (fresh env), `/api/health` gate, system Postgres via apt (no docker dependency), authenticated git clone via workflow `GITHUB_TOKEN`. |
| **Tests** | 50 unit tests using `node:test` across 5 files (validation, time entries, Ask LLM, inspections/meetings/risks, toolbox/maintenance/suppliers). CI runs them between tsc + build. |
| **Lint** | ESLint 9 + `next/core-web-vitals` preset (flat config at `eslint.config.mjs`) wired into CI as a hard gate. 0 errors. 24 React-19-compiler-grade warnings remain — all on the canonical client-side fetch-then-set pattern (`react-hooks/set-state-in-effect`) inside page-level effects, intentionally tolerated until we adopt Suspense / RSC data fetching (rationale documented in the config). 3 fixable instances cleaned up in `useRealtimeActivity`, `DashboardSwitcher` (URL-param sync via the React-19 prev-state sentinel pattern) and `Focus` (timer clamp-to-0 instead of branching to sync setState). |
| **Drawings + Schedule** | `/drawings` — `Drawing` + `DrawingRevision` models, per-project `DWG-NNN` sequencing, per-drawing unique `(drawingId, revision)`, file storage via existing `/api/uploads` (PDF / image / DWG ≤ 25 MB), revision history with "CURRENT" tag, status pipeline draft → approved → superseded → archived, auto-incrementing rev labels (A→B, P01→P02), discipline + project filter chips. **/schedule** — read-only `/api/schedule?from=&weeks=` aggregating Tasks-with-dueDate across active projects. Horizontal Gantt-style day grid with sticky project column, today highlighted, Monday boundaries thicker, weekend dimmed. Per-task bars positioned by dueDate with status background + priority left-stripe, clickable to `/tasks?focus=`. Prev/next-week, 4/8/12-week zoom, project filter. |
| **Valuations** | `/valuations` — derived view (no new model) for UK construction interim payment applications. `/api/valuations?retentionPct=` aggregates active projects with `budget > 0` and `progress > 0`, computes `gross = budget × progress/100`, less `retentionPct` retention and previous-certified payments, returns `netDue` per project plus workspace totals. UI shows per-project VAL-NNN cards with status pill (draft/submitted/certified), full breakdown table, configurable retention input (0-20%, default 3%). "Preview only — not certified" banner makes the indicative nature clear. Ported from `archive/cortexbuild-field/app/valuations.tsx` (Manus) to the Next.js stack. Drives the `Money & ops` apps section. |
| **Safety incident register** | `/safety` — `SafetyIncident` model + migration (`20260524001100_add_safety_incident`). 6 incident types per UK practice (near miss / first aid / accident / dangerous occurrence / environmental / security), 5 severity levels, 3-stage status pipeline (open → investigating → closed). RIDDOR-aware: auto-flags accident / dangerous occurrence / critical-severity incidents as RIDDOR-reportable (overridable). Days-without-incident counter, open + RIDDOR-open KPI strip. Activity events on log / close / delete. 10-test parser suite covers auto-RIDDOR rules, days-without-incident math, and the status-transition guard. Ported from `archive/cortexbuild-field/app/safety.tsx` (Manus) to the Next.js stack — replaces the react-native MOCK data with a real Postgres-backed model. |

## 🗺 v1.1 — legacy-archive port plan (May 26 2026)

An Explore-agent audit of `archive/` (10 sub-folders, ~5 MB of historical
codebases) produced this concrete punch list of features that exist in
one or more archived versions but are missing from production. Items
are ordered by impact/effort ratio. Architecture-blocker archives
(`cortexbuild-unified` failed monorepo, `cortexbuildpro` duplicates,
`cortexbuild-platform` infra-only) are skipped.

| # | Item | Source archive | Effort | Status |
|---|------|---------------|--------|--------|
| 1 | **CSV ledger export** (Xero / QB / Sage compatible) | cortexx-pwa | TRIVIAL | ✅ `1bd6f1d` |
| 2 | **Project bookmarks** (user-saved shortcuts) | cortexbuild-field | TRIVIAL | ✅ `1bd6f1d` |
| 3 | **Activity feed enhancements** (live SSE stream, filters) | cortexbuild-ultimate | SMALL | ✅ `8fc25d7` |
| 4 | **Customer portal hardening** (share-token revoke, expiry) | cortexx-pwa | SMALL | ✅ `838b14e` |
| 5 | **Action plans module** (create / track / close-out) | cortexbuild-field | MEDIUM | ✅ `52f7961` |
| 6 | **Conflict resolution log** (cross-team conflicts on site) | cortexbuild-field | MEDIUM | ✅ `a0c430a` |
| 7 | **Cost forecasting** (cost-vs-actual trend chart on /reports) | cortexbuild-field | MEDIUM | ✅ `26534ce` |
| 8 | **CIS300 monthly return automation** | cortexbuild-field | MEDIUM | ✅ `e2493d8` |
| 9 | **Team chat + conversation memory** | cortexbuild-web | MEDIUM | ✅ `d2ca203` |
| 10 | **Real-time Notification Center** (in-app inbox, unified) | cortexbuild-ultimate | MEDIUM | ✅ `76ea2b7` |

### Parallel R&D (v2.0 horizon)

| Item | Source archive | Effort | Note |
|------|---------------|--------|------|
| Advanced analytics dashboards (BI) | cortexbuild-ultimate | LARGE | 8–12 weeks |
| AI Agent expansion (8 personas) | cortexbuild-ultimate | LARGE | 6–8 weeks; superset of Vera CEO/autopilot |
| Photo-as-mention (vision → action items) | cortexx-pwa | MEDIUM | Vision pipeline is already wired |
| Offline map download & mark-up | cortexx-pwa | MEDIUM | iOS Capacitor + Mapbox |
| NFC site check-in | cortexx-pwa | SMALL | Capacitor plugin |
| StoreKit + Stripe subscription parity | cortexx-pwa | MEDIUM | iOS in-app purchase |
| Equipment service logs | cortexbuild-field | SMALL | Maintenance schedule already exists; extend |
| Enquiry pipelines (sales funnel) | cortexbuild-field | MEDIUM | Lead → quote → win |

### Non-starters (skip)

- **Mobile Expo port to Next.js** (cortexbuild-field): XLARGE; keep Expo
  app as a parallel deployment if it ever ships
- **MySQL → PostgreSQL rewrites** (cortexbuild-web): full re-build,
  not a port
- **Failed monorepo unification** (cortexbuild-unified): aspirational
  README, real code 50% non-existent. Lessons learned via decisions,
  not code.

## 🛠 In flight

| Item | Status |
|---|---|
| **Voice → text transcription** | ✅ **Shipped.** `POST /api/transcribe` accepts a same-origin `/api/uploads/*` audio URL, converts to 16 kHz mono WAV via `ffmpeg`, runs `whisper.cpp` (`base.en` model) and returns the transcript. `/capture` voice flow now calls it automatically after audio upload — successful transcription becomes the RFI task description (`Transcript:\n…\n\nAudio: …`); on `WHISPER_UNAVAILABLE` / `FFMPEG_UNAVAILABLE` / `EMPTY_TRANSCRIPT` the task still gets created with just the audio link. Deploy workflow now installs `ffmpeg` + `build-essential` and clones / builds `whisper.cpp` at `/opt/whisper.cpp` (idempotent, best-effort — failure doesn't break deploy). SSRF-safe (URL allowlist), 90s timeout, 25 MB audio cap. |
| **Real-time dashboard cross-tab sync** | ✅ **Shipped.** `lib/broadcast.ts` wraps the BroadcastChannel API with two message types (`data:invalidate` per-scope, `auth:signout`). `useDashboardData` listens, plus `/tasks`, `/projects`, `/team` each subscribe to their own scope and refetch on receipt. Successful mutations on those pages call `broadcastInvalidate(scope)` so sibling tabs update in ~50ms instead of waiting 200–500ms for the SSE round-trip. SSR-safe (no-op without `window`/`BroadcastChannel`). 6 unit tests stubbing the global. |
| **Capacitor → Next.js bridge** | ✅ **Shipped.** Both modes live, toggled by a single `CAPACITOR_SERVER_URL` env: unset → offline single-file PWA bundle (existing `public/legacy/`); set → cloud mode (`server.url` in `capacitor.config.ts` points the WKWebView at the live Next.js app on every launch — full backend, always up to date, recommended for TestFlight). `scripts/build-web.mjs` reads the same env to write either the placeholder `index.html` (cloud) or the full legacy copy (offline). `ios/README.md` rewritten with the two flows side-by-side. |
| **AI enhancements per module** | ✅ **Complete.** Base modules real; AI cherries-on-top all shipped. Text: AI-drafted quotes + AI-estimator on POs (Ollama, `format: "json"`, shared `lib/llmDrafts.ts` parser + 13-test suite); Whisper transcription on voice RFI (`whisper.cpp` + `ffmpeg`); weather auto-fetch on site-diary (wttr.in proxy + 30-min cache); PDF export across quotes, invoices, POs, and sub-invoices (pdfkit, A4 branded layouts with status pills + CIS breakdown). **Vision** (Ollama `moondream` ~1.8 GB, idempotently pulled by deploy): `/api/snags/[id]/analyze` (defect detection, severity rubric, 12-test parser suite); `/api/documents/[id]/tag` (category + 1-8 trade-speak tags + summary, `/photos` lightbox); `/api/photos/compare` (two-image comparison, progressed/reversed/stalled/unrelated verdict, dedicated compare-mode UX); `/api/drawings/[id]/compare` (revision-vs-revision design diff, severity rubric, structural/MEP/finishes/layout/annotation classification, designIntent + reviewRecommended flags, inline compare UI on the drawing detail sheet). Plus the `/ask` dashboard variant now hits the real `/api/ask` Ollama endpoint (previously a hardcoded keyword router — fixed in this round). |
| **Next.js 14 → 16 migration** | ✅ **Merged to main** (PR [#23](https://github.com/adrianstanca1/cortexx/pull/23), squash-commit `ac11c37`). Bumps Next 14.2.35 → 16.2.6, React 18 → 19, ESLint 8 → 9 (flat config), eslint-config-next 14 → 16. 50 route handlers migrated to async `params: Promise<{...}>` via a one-shot minimum-diff script. Follow-up commits: `435fc90` renames `middleware.ts → proxy.ts` (Next 16 deprecation); PR [#24](https://github.com/adrianstanca1/cortexx/pull/24) resolves the 7 `react-hooks/purity` warnings via `useState`'s lazy initializer (the React-19-safe pattern). Dependabot PR [#22](https://github.com/adrianstanca1/cortexx/pull/22) closed as superseded. Production runs Next 16 — Health Monitor green across all post-merge cycles. |
| **SaaS foundation (Phase 1)** | ✅ **Data layer + APIs + UI deployed to production.** Schema migrations applied: `Organization`, `UserOrganization`, `OrganizationInvite`, `AuditEvent`, `NotificationPreference` tables (`20260525000000`); `organizationId` column + FK + index added to all 39 owned models (`20260525010000`) after a follow-up migration (`20260525005000`) created four missing physical tables (`Milestone`, `Permit`, `Rams`, `Tender`) that were in the schema but never got CREATE TABLE migrations. Default workspace (slug `cortexbuildpro`) seeded with admin user as `owner`. Helpers: `lib/{org,rbac,audit,email,billing,cron,tenancy}.ts`. New routes: `/api/orgs` (list/create/switch), `/api/orgs/[id]/{invites,members,members/[memberId],audit-log}`, `/api/invites/[token]` (public GET + auth POST), `/api/notifications/preferences`, `/api/billing/{checkout,portal}`, `/api/webhooks/stripe`, `/api/cron/{overdue-invoices,prune-push,expiry-warnings}`. New pages: `/onboarding`, `/invite/[token]`, `/settings/organization` (members + invites + Stripe plan cards + audit-log link), `/settings/audit-log`, public `/pricing`. Service worker push helpers now honour per-user category preferences (tasks / safety / invoices / announcements). Auth.js v5 session callback caches org memberships on the JWT. Prisma client extension `tenancyExtension` is wired but gated behind `MULTITENANT_ENFORCED` env flag (default off) — flips to on once a codemod adds the `organizationId` filter to all 122 owned-route handlers. Deploy workflow now self-heals stuck Prisma migrations and reuses the existing DB password if it still authenticates (prevents the password-drift outage that happened during this rollout). |
| **Deploy reliability — atomic password rotation** | ✅ Discovered during Phase 1 deploy: failed deploys were rotating the postgres password but not writing the matching `.env.production`, drifting them apart and bricking subsequent deploys. Fix: the deploy script now reads the existing password from `.env.production` and authenticates against postgres before deciding whether to rotate. If the existing password still works, keep it; only rotate if missing/broken. Atomic: DB password and `.env.production` always match by the end of every deploy, regardless of where the script aborts. New `db-rescue.yml` workflow (inspect / clear-stuck / clear-all / resync-password actions) provides a manual escape hatch for future drift; `deploy-diagnose.yml` + `migrate-debug.yml` write the diagnostic output back to `ops/last-{diagnostic,migrate}.txt` in the repo so failures are debuggable from anywhere with `git pull`. |
| **Zero-touch multi-tenant auto-scoping** | ✅ `requireAuth()` now sets the AsyncLocalStorage org context — every existing route that already authenticates gains automatic Prisma org-scoping for free once `MULTITENANT_ENFORCED` flips on. The Prisma extension reads the context and injects `organizationId` into every find / count / aggregate / groupBy / updateMany / deleteMany / create / createMany / upsert / update / delete on the 39 owned models. Crucially: the extension clones args at every branch (never mutates the caller's object — that bug would leak orgIds across reused-template calls), fails closed when no context is set (throws a clear error pointing at `bypassTenancy()`), and overrides any user-supplied `organizationId` so spoofing in `where` or `data` is impossible. `bypassTenancy()` wraps the 4 routes that legitimately operate across tenants: `/api/cron/{overdue-invoices,expiry-warnings}` (per-org grouping, no cross-tenant leak), `/api/webhooks/stripe` (org resolved from Stripe customer id), `/api/client-view/[token]` (share-token = cross-org by design), `/api/seed` (one-shot reseeder). 17 dedicated unit tests for the extension cover the transformation, the clone guarantee, and cross-tenant attack scenarios (user supplying `organizationId` in where/data is overwritten). |
| **Phase 1 critical security fixes** | ✅ A medium-effort code review of the multi-tenancy rollout surfaced 5 real bugs, all fixed before they could fire (`MULTITENANT_ENFORCED` is still false, but four would have silently expanded into data leaks the moment it flipped). 1) `/api/webhooks/stripe` trusted `session.metadata.organizationId` in isolation — attacker could mint a checkout session with a victim's orgId in metadata and hijack their plan; now resolves the org from the Stripe customer id and asserts any claimed orgId matches; pinned `runtime: 'nodejs'` so raw body bytes survive intact for HMAC verification. 2) `/api/cron/overdue-invoices` built one all-tenants list and emailed it to every user with `invoicesEmail=true` — direct cross-tenant PII / financial leak; now groups by org, each digest scoped to its own workspace's members + prefs. 3) `lib/tenancy.ts` extension mutated `args.data` / `args.where` in place — shared templates would carry leaked organizationIds into later calls; cloned at every branch instead. 4) Same extension passed through silently when no org context was set on an owned-model query — now fails closed with a remediation pointing at `bypassTenancy()`. 5) `/api/notifications/preferences` GET performed an upsert — broke the GET-is-safe HTTP invariant + opened a CSRF mutation vector; GET is now read-only with defaults, PUT is the single write path. |
| **Hardening (Phase 3)** | ✅ Rate limiting on every authenticated write endpoint (99 routes via `scripts/add-rate-limits.mjs` codemod — adds `enforceRateLimit(req, 'write', userId)` right after the auth guard, 60 req/min/user with `Retry-After` 429s). Security headers on every page response: `Strict-Transport-Security` (2 years + preload), `X-Content-Type-Options: nosniff`, `X-Frame-Options: SAMEORIGIN`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` locking camera / microphone / geolocation to same-origin. Sentry scaffold (`lib/sentry.ts` + `instrumentation.ts`) — initialised when `SENTRY_DSN` is set, no-op otherwise; scrubs Authorization / Cookie / VAPID headers before transport; `/api/errors` forwards client-side errors to Sentry while keeping the stdout log as the canonical record. `proxy.ts` middleware now redirects authenticated users without an active org to `/onboarding` (or 403 with code `NO_ORG` on API), closing the last edge case for the post-codemod multi-tenant rollout. |
| **Performance scaffolding (Phase 5)** | ✅ `@next/bundle-analyzer` wired in — `ANALYZE=true npm run build` opens the client/server bundle treemap (sets up the Phase 5.1 bundle-size budget CI gate). Core Web Vitals: `web-vitals` package + `components/WebVitalsReporter.tsx` (client-only, dynamic-imported, sampled at `NEXT_PUBLIC_VITALS_SAMPLE_RATE` default 10%) reports CLS / FCP / INP / LCP / TTFB to `/api/metrics` via `navigator.sendBeacon` (survives unload). The endpoint is public + IP rate-limited; logs structured JSON to stdout for now (pluggable to Plausible / Loki / ClickHouse without changing the beacon shape). Postgres FTS: migration `20260525020000_add_fts_search_columns` adds a STORED `tsvector` column + GIN index to Project / Task / Invoice / Document / Snag / Rfi / Customer / Subcontractor with `setweight()` per field. Generated columns rebuild on every row update — zero code change in writes. `/api/search` can swap to `searchVector @@ websearch_to_tsquery($1)` for sub-50ms queries on 100k+ rows in a follow-up. |
| **Marketing landing + standalone PWA from the Claude Design canvas** | ✅ Cortexx Marketing.html (from project `019ddd6b-32b0-7b27-809c-b50bac72a737` on Claude Design) promoted to `public/marketing.html`, served at `/marketing` via a Next.js rewrite and at `/` for signed-out visitors via auth-aware redirect in `app/page.tsx`. Every CTA on the page (Sign in, Get started, Try it →, Try the live demo, Start Pro free, Talk to sales, footer Privacy/Terms/Support) rewritten from the dead `Cortexx.html` link to the real SaaS surfaces. `/pricing` footer linked back to it. Plus the fully-inlined 1.7 MB standalone PWA (all 80 phases bundled into one file, offline-forever) and its 1.6 MB deploy variant copied into `public/legacy/` — both now reachable at `/legacy/Cortexx-{standalone,deploy}.html`. Nothing from the Claude Design canvas project is unreachable on cortexbuildpro.com. |
| **TOTP 2FA + Help docs** | ✅ End-to-end TOTP enrolment + verification: `lib/totp.ts` (speakeasy + qrcode + bcrypt), 4 routes under `/api/auth/2fa/{setup,enable,disable,verify}`, `/settings/security` UI with QR scan, 6-digit verify, 10 single-use backup codes (case-insensitive, whitespace-tolerant, single-use enforced by splice-out), password-gated disable. All routes rate-limited (`auth` profile), every transition writes `AuditEvent`. 12 new unit tests cover format regex, backup-code shape + single-use invariant, fast length-precheck short-circuit. Plus `/help` index + `/help/[slug]` with five articles (Getting started · Team roles · AI tools · Plans & billing · Security & data); statically pre-rendered via `generateStaticParams`. |
| **Claude Code session-start hook** | ✅ `.claude/hooks/session-start.sh` + `.claude/settings.json` — every Claude Code on the web session now boots with `npm install` + `npx prisma generate` already done. Gated on `$CLAUDE_CODE_REMOTE=true` so local dev machines are unaffected. Synchronous (no `async:true`) so the agent never hits a stale Prisma client or missing module on its first tool call. Validated end-to-end: hook script runs clean (~3 s cold / ~200 ms warm), eslint runs clean, the rbac-org test suite passes after the hook completes. |
| **v1.0.0 launch** | ✅ Tagged `v1.0.0` (commit `11ce98c`). The 10-item launch checklist landed: cross-org integration suite (`bbe978d`, 10/10 green in CI), `MULTITENANT_ENFORCED=true` in production (`9420bbd`, deploy-verified), Stripe price-id startup validation (`6575ae2`), backup-restore verification cron with auto-issue-on-failure (`ebcf4af`), `auditLog()` codemod across 36 DELETE routes (`6575ae2`), S3 / object-storage adapter with `downloadToTemp()` for AI pipelines (`6147bb3`), iOS App Store release pipeline `release-ios.yml` tag-triggered on `v*-ios` (`8c35630`), `CHANGELOG.md` published (`11ce98c`). pm2 cluster mode + Redis and CSP nonces deferred as 🟨 polish for v1.1. |
| **Legacy-parity v1.1: 24 modules added** | ✅ Every screen from the 80-phase Claude Design PWA now has a real surface in the SaaS app. 24 new Prisma models + 24 generated pages + 24 API routes + `components/ui/ModuleShell.tsx` shared frame. Modules: Payroll · Holiday & leave · Bank reconciliation · Carbon log · Waste log · Performance · Apprentices · Sub portal · Insurance claims · Currency · Service catalog · Site reviews · Goals (OKRs) · Improvement hub · Kaizen board · Templates · Forms · Reminders · Saved views · Tags · Process library · Personas · Developer API · Infrastructure. All routed via `/apps` in five extended categories. Each opens with list + create-record (free-form prompt on the first field); per-module richer CRUD lands when the product spec for that surface lands. Total schema models 49 → 73. Tenancy extension extended to cover the 24 new owned models. Generator `scripts/generate-legacy-parity-modules.mjs` kept in-repo so the same shape can be replayed. |

## 🎯 What's shipped (all 24 modules + extras)

Every roadmap module is now a real implementation with auth-gated CRUD and matching UI. Quick map:

- **Comms**: `/messages` (Announcement), `/rfis` (Rfi), `/ask` (local Ollama LLM)
- **Sales**: `/leads` (Lead), `/customers` (Customer), `/quotes` (Quote, line items), `/client-view` + public `/client/[token]`
- **Site ops**: `/schedule` (Gantt), `/site-diary` (aggregation), `/photos` (Document), `/drawings` (Drawing + DrawingRevision), `/snags` (Snag), `/observations` (Observation), `/variations` (Variation)
- **Money & ops**: `/pos` (PurchaseOrder), `/sub-invoices` (SubInvoice with HMRC-CIS auto-calc), `/materials` (Material), `/subs` (Subcontractor), `/equipment` (Equipment), `/cost-catalog` (CostItem), `/mileage` (MileageEntry)
- **People & time**: `/timesheets` (TimeEntry weekly grid), `/check-in` (SiteCheckIn with GPS), `/live-status` (real-time map), `/training` (Certification)

## 🐛 Known issues

| # | Issue | Severity | Notes |
|---|---|---|---|
| 1 | ~~Capture page only stores file metadata, not bytes~~ | ✅ Fixed | `/api/uploads` POST writes to `UPLOAD_DIR` (default `./uploads`, prod = `/var/lib/cortexx/uploads`); `/api/uploads/[name]` streams it back (auth-gated). `Document` has `url`/`size`/`mimeType`. `/capture` uploads then creates the doc; `/documents` shows thumbnails for images. 25 MB cap, MIME allowlist. |
| 2 | ~~Voice RFI: audio captured, transcription still pending~~ | ✅ Fixed | `/capture?type=voice` records via MediaRecorder, uploads the blob, then calls `/api/transcribe` which runs `whisper.cpp` (`base.en` model) after an `ffmpeg`-driven 16 kHz mono WAV conversion. Successful transcription becomes the RFI task description; on `WHISPER_UNAVAILABLE` / `FFMPEG_UNAVAILABLE` / `EMPTY_TRANSCRIPT`, the task still gets created with the audio URL. Deploy workflow installs whisper.cpp idempotently. |
| 3 | ~~Module stubs don't surface their "coming soon" status~~ | ✅ Fixed | All 24 modules are now real implementations — zero `ModuleStub` references left in `/app/`. |
| 4 | Client-rendered pages → weak social previews | Partial | Root layout now exports `openGraph` + `twitter` metadata using `NEXT_PUBLIC_SITE_URL`, so unfurls work even for client-rendered routes. Full server-component refactor stays an open architecture decision. |

## 🚀 Production deploy checklist

Run these after every push to main (the Hostinger workflow handles 1+2 automatically):

1. **Run migrations** — `prisma migrate deploy` (currently 14 migrations; the latest 9 were added this rebuild cycle)
2. **Restart pm2** — `pm2 reload cortexx` (or `pm2 delete cortexx && pm2 start ...` for env-var changes)
3. **Health gate** — wait for `/api/health` → 200 before declaring deploy good
4. **Local LLM** (only if `/ask` should respond):
   - `curl -fsSL https://ollama.com/install.sh | sh`
   - `ollama pull llama3.2:3b` (or whatever `OLLAMA_MODEL` env is set to)
   - `systemctl enable --now ollama` (or `ollama serve` in tmux)

## 🏗 Architecture decisions

1. **No docker on the VPS** — system Postgres via apt. We tried docker-Postgres; the VPS got wiped and we couldn't recover docker. System Postgres + peer auth + pm2 is simpler and one fewer moving part.
2. **Server components by default** — client components only where state, effects, or browser APIs are needed. Cuts JS bundle.
3. **`force-dynamic` on every GET API route** — all routes are auth-gated; Next.js's default caching would cache pre-auth responses.
4. **Service worker network-first for navigation** — never serve stale HTML. Static assets are stale-while-revalidate so they cache for ~30d but update in background.
5. **Cross-tab realtime via SSE + BroadcastChannel** — SSE remains the source-of-truth path (server pushes activity events; tabs refetch). BroadcastChannel (`lib/broadcast.ts`) is layered on top for same-browser sibling tabs: a successful mutation in tab A broadcasts `data:invalidate` so tab B refetches in ~50 ms instead of waiting for the server hop. Scoped (`tasks`/`projects`/`team`/`activity`/`all`) to keep invalidations targeted.
6. **No design system / no Tailwind classes for the dashboard** — inline styles match the Claude design's prototype exactly. Refactoring to Tailwind happens once we stabilise the visual language (post-v1.5).

## 🅿️ Parked

| Item | Why parked |
|---|---|
| ~~**iOS native build (Capacitor wrap)**~~ | ✅ Unblocked — Capacitor 6 scaffold + App Store submission pack now live in this repo (`ios/`, `app-store/`). Last-mile work: adapt `ios/scripts/build-web.mjs` to run `next export` (or point Capacitor at the deployed `cortexbuildpro.com` via `server.url`) instead of copying the standalone PWA HTML. |
| ~~**Push notifications**~~ | ✅ Shipped. `PushSubscription` model + migration, web-push VAPID delivery via `lib/push.ts` with stale-endpoint pruning, `/api/push/subscribe` + `/api/push/test`, Settings UI toggle. Service worker `cortexx-v3` handles `push` + `notificationclick`. Activates the moment an admin sets `VAPID_PUBLIC_KEY` + `VAPID_PRIVATE_KEY` env vars (subscriptions captured before keys are set still work — no re-subscribe needed). |
| **Multi-tenancy** | Currently single-tenant by design. Adding `organizationId` is not a one-session refactor — it touches 44 Prisma models, ~120 API route handlers, every list query in every page, every activity event, every cross-resource link (e.g. `Invoice.project` must stay in-org), every realtime SSE event filter, and needs a data-migration step to backfill existing rows into a default org plus a row-level-security pass to prevent cross-org leaks. Punting this to a dedicated sprint with a written design doc rather than smuggling in a half-baked foundation that silently leaks data between hypothetical future orgs. |
| ~~**Cortex AI agent**~~ | ✅ Shipped (`/ask`) — runs against a local Ollama LLM (default `llama3.2:3b`). Workspace-context system prompt grounds answers in live project/snag/timesheet/activity data. No external API key; no per-request cost. |
