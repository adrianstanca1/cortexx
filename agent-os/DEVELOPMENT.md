# Development

Run `npm run typecheck`, `npm test`, and `npm run build` before merging. Keep runtime packages dependency-light. Add destructive tools only with explicit permission declarations and tests. Provider adapters must implement health, models and generate. All externally-derived research results should retain provenance when the research subsystem is expanded.