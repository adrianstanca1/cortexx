run_id: 30238195944
exit_code: 255
completed_at: 2026-07-27T04:51:52Z
command: |
  F=/root/.hermes/scripts/cortexx_db_backup.sh
  cp "$F" "/root/hermes-db-backup.bak-$(date +%Y%m%d-%H%M%S)"
  python3 - <<'PY'
  import re
  F="/root/.hermes/scripts/cortexx_db_backup.sh"
  s=open(F).read()
  if "docker compose" in s:
      print("already patched"); raise SystemExit
  old='  if sudo -u postgres pg_dump --format=custom --dbname="$PG_DB" --file="$dump" 2>/dev/null; then'
  new='  # PATCHED 2026-07-27: was `sudo -u postgres pg_dump` against the HOST\n  # Postgres — an orphaned DB with 83 tables and 0 rows. The live database\n  # is inside the `db` container (both cortexx-api-1 and cortexx-admin-1\n  # use db:5432/cortexx). Custom format is preserved so the existing\n  # replica + Telegram/S3 replication steps keep working unchanged.\n  if ( cd /opt/cortexx && docker compose exec -T db pg_dump -U postgres --format=custom -d "$PG_DB" ) > "$dump" 2>/dev/null && [ -s "$dump" ]; then'
  assert old in s, "anchor line not found"
  s=s.replace(old,new,1)
  open(F,"w").write(s)
  print("patched")
  PY
  bash -n "$F" && echo "syntax OK"
  echo "--- running it:"
  bash "$F"
  echo "--- verify newest .dump is the REAL db (expect ~45 tables):"
  D=$(ls -t /var/backups/cortexx/*.dump | head -1); echo "file: $D ($(du -h "$D" | cut -f1))"
  docker exec -i cortexx-db-1 pg_restore -l < "$D" 2>/dev/null | grep -c "TABLE " || echo "(pg_restore -l failed)"
  
---
ssh: connect to host 72.62.132.43 port 22: Connection timed out
